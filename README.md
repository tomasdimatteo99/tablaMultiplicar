# Notas:
```
Options:
      --help     Show help                               [boolean]
      --version  Show version number                     [boolean]
  -b, --base     Base de la tabla de multiplicar.
                                               [number] [required]
  -f, --factor   Factor de limite para la tabla.
                                            [number] [default: 10]
  -l, --listar   Muestra la tabla en consola.
                                        [boolean] [default: false]
```